
/* Enum: EnumType

	When topics are generated there will be embedded ones added following EnumType to allow each
	value to have a symbol and be linkable.  However, that means the prototype technically comes after
	C instead of EnumType.  The prototype must still be applied to EnumType.

	A - Value A
	B - Value B
	C - Value C
*/
public enum EnumType
	{ A, B, C }
